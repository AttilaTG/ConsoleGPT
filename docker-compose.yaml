services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
          
  consolegpt:
    image: consolegpt
    build:
      context: .
      dockerfile: ConsoleGPT/Dockerfile
    depends_on:
      - ollama
    environment:
      - OLLAMA_HOST=http://ollama:11434
    stdin_open: true
    tty: true
    restart: unless-stopped

volumes:
  ollama_data:
